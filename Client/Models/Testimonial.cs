namespace Nexo.Client.Models
{
    /// <summary>A client/artist quote shown in the commercial Home's testimonials section.</summary>
    public record Testimonial(string Quote, string Author, string Role);
}
