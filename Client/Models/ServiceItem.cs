namespace Nexo.Client.Models
{
    /// <summary>A service JCB Studio offers to clients, shown on the commercial Home.</summary>
    public record ServiceItem(string Icon, string Title, string Description);
}
