namespace Nexo.Client.Models
{
    /// <summary>A featured production/artist shown in the commercial Home's portfolio grid.</summary>
    public record PortfolioItem(string Title, string Genre, string AccentFrom, string AccentTo);
}
